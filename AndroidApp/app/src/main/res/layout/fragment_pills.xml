<!-- res/layout/fragment_hygiene.xml -->
<androidx.core.widget.NestedScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".PillsFragment">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!-- Título arriba del todo -->
        <TextView
            android:id="@+id/pillsTopTitleTextView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/pillsTitle"
            android:textSize="18sp"
            android:textAlignment="center"
            android:textStyle="bold"/>


        <!-- Descripción justo debajo del título -->
        <TextView
            android:id="@+id/pillsDescriptionView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/text_pills"
            android:layout_below="@id/pillsTopTitleTextView"
            android:layout_marginTop="16dp"
            android:textSize="18sp"
            android:textAlignment="center"/>


        <TextView
            android:id="@+id/listTextView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@id/pillsDescriptionView"
            android:layout_marginTop="16dp"
            android:text="@string/pills_steps"
            android:textSize="16sp"
            android:textColor="@android:color/black"/>

        <!-- Imagen entre la descripción y el siguiente título -->
        <!-- Imagen entre la descripción y el siguiente título -->
        <ImageView
            android:id="@+id/staticImageView"
            android:layout_width="200dp"
            android:layout_height="200dp"
            android:layout_below="@id/pillsDescriptionView"
            android:layout_marginStart="16dp"
            android:layout_marginTop="230dp"
            android:layout_marginEnd="16dp"
            android:layout_marginRight="0dp"
            android:layout_marginBottom="16dp"
            android:src="@drawable/pills"
            android:layout_centerInParent="true"/>

    </RelativeLayout>
</androidx.core.widget.NestedScrollView>
